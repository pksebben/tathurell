# Eval + dev tooling. Layers on the runtime deps so they're defined in one place.
-r ../requirements.txt

# Metrics / corpora / test harness (not needed to run the tool)
jiwer
meeteval
pyannote.metrics
datasets
soundfile
pydub
pytest

# Alternative engines used only by the bake-off / parked seams
vosk
openai-whisper
mlx-whisper
